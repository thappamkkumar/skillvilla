import { lazy } from 'react';
import { Route, Navigate } from 'react-router-dom';
  

const LivesRoutes = () => (
    <> 
			 <Route path="lives" element={<h1> Live </h1>} />
										
		</>
);

export default LivesRoutes;
